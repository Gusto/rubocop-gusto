# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem "rake"
gem "rspec"
gem "rubocop-graphql", ">= 1.8.0" # config/graphql.yml configures its cops; not a gem dependency
gem "rubocop-rails" # ensures tests pass that expect rubocop-rails to be installed
gem "simplecov", require: false, group: :test
